# Fix "File Not Found" (404) Error

## ✅ Server Status
The server IS running on port 8000.

## 🔧 Steps to Fix

### Step 1: Verify Server is Running
Open a NEW PowerShell window and run:
```powershell
cd "C:\Users\nunus\OneDrive\Desktop\python project"
python -m http.server 8000
```

You should see:
```
Serving HTTP on :: port 8000 (http://[::]:8000/) ...
```

**DO NOT CLOSE THIS WINDOW** - Keep it open while using the app.

### Step 2: Access the Correct URL

Open your browser and go to:
- ✅ **http://localhost:8000/index.html**
- ✅ **http://localhost:8000/** (should auto-load index.html)
- ✅ **http://127.0.0.1:8000/index.html**

**NOT:**
- ❌ file:///C:/Users/... (file:// protocol won't work)
- ❌ http://localhost:8000 (without /index.html might not work)

### Step 3: Clear Browser Cache

If you still see errors:
1. Press `Ctrl + Shift + Delete`
2. Clear cached images and files
3. Or use **Incognito/Private mode** (Ctrl+Shift+N)

### Step 4: Test with Simple Page

First, test if server is working:
- Go to: **http://localhost:8000/test.html**
- You should see "Server is working! ✓"

If test.html works but index.html doesn't, there might be an issue with index.html.

## 🐛 Debugging

### Check Browser Console
1. Press `F12` to open Developer Tools
2. Go to **Console** tab
3. Look for red errors
4. Go to **Network** tab
5. Refresh the page (F5)
6. Check if index.html loads (should show 200 status)

### Verify Files Exist
In PowerShell (in project directory):
```powershell
dir *.html
```

Should show:
- index.html
- test.html

## 📋 Complete Working Example

1. **Open PowerShell**
2. **Run these commands:**
   ```powershell
   cd "C:\Users\nunus\OneDrive\Desktop\python project"
   python -m http.server 8000
   ```
3. **Keep PowerShell window open**
4. **Open browser** → http://localhost:8000/index.html
5. **Wait 10-30 seconds** for Pyodide to load

## ⚠️ Important Notes

- The server MUST run from the project directory
- Keep the PowerShell window open while using the app
- Use `http://localhost:8000/index.html` (not file://)
- First load takes 10-30 seconds (Pyodide downloading)

## 🆘 Still Not Working?

1. Check if port 8000 is free:
   ```powershell
   netstat -ano | findstr :8000
   ```

2. Try a different port:
   ```powershell
   python -m http.server 8080
   ```
   Then access: http://localhost:8080/index.html

3. Check Windows Firewall isn't blocking localhost

4. Try a different browser (Chrome/Edge recommended)

